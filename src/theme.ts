import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#463f3a",
    },
    secondary: {
      main: "#8a817c",
    },
  },
});

export default theme;
